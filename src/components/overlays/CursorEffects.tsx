/**
 * CursorEffects — disabled (using normal system cursor).
 * Kept as a no-op component so lazy-import references in App.tsx still resolve.
 */
export default function CursorEffects() {
  return null;
}
